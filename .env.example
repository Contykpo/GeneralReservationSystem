ACCEPT_EULA=Y
SA_PASSWORD=<SQL Server SA Password>

SQL_HOST=sqlserver
SQL_PORT=1433
SQL_DB=<DB Name>
SQL_USER=<DB User>
SQL_PASSWORD=<DB User Password>

ADMIN_USER_PASSWORD=<Admin Password>
APP_USER_PASSWORD=<App User Password>

JWT_SECRET_KEY=<Generate a secure random string of at least 64 characters>

# CORS Configuration - Allowed Origins (comma-separated)
# Example: http://localhost:5000,https://localhost:5001,https://myapp.com
CORS_ORIGINS=<Comma-separated list of allowed origins>