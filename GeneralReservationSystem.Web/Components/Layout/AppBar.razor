@* 
<MudContainer>
    <MudAppBar Elevation="1" @rendermode="InteractiveServer">
        <MudText Typo="Typo.h5" Class="ml-3">GRS</MudText>
        <MudSpacer />
        <MudIconButton Edge="Edge.End" OnClick="@((e) => DrawerToggle())">
            <MudAvatar Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Size="Size.Medium">U</MudAvatar>
        </MudIconButton>
    </MudAppBar>

    <MudDrawerContainer>
        <MudDrawer @bind-Open="_drawerOpen" Anchor="Anchor.Start" ClipMode="DrawerClipMode.Always" Elevation="2">
            <NavMenu />
        </MudDrawer>
    </MudDrawerContainer>

    <MudMainContent>
        @Body
    </MudMainContent>
</MudContainer>

@code
{
    private 

    private bool _drawerOpen = false;

    void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }
} *@
