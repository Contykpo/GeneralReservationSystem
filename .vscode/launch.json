{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Docker Compose",
            "type": "docker",
            "request": "attach",
            "platform": "netCore",
            "sourceFileMap": {
                "/src": "${workspaceFolder}"
            }
        }
    ]
}